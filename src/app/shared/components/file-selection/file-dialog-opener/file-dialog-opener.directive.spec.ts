import { FileDialogOpenerDirective } from './file-dialog-opener.directive';

describe('FileDialogOpenerDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDialogOpenerDirective();
    expect(directive).toBeTruthy();
  });
});
